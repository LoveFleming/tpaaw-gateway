{
  "name": "tpaaw-gateway",
  "version": "0.1.0",
  "description": "PAAW Gateway — install / update / run PAAW (bootloader)",
  "type": "module",
  "bin": { "paaw-gateway": "gateway.mjs" },
  "scripts": {
    "dev": "node gateway.mjs",
    "start": "node gateway.mjs",
    "update": "node gateway.mjs update",
    "status": "node gateway.mjs status"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "engines": { "node": ">=20" }
}
