{
  "name": "tpaaw-gateway",
  "version": "0.2.1",
  "description": "PAAW Gateway \u2014 install / update / run PAAW (bootloader)",
  "type": "module",
  "bin": {
    "paaw-gateway": "gateway.mjs"
  },
  "scripts": {
    "dev": "node gateway.mjs ui",
    "start": "node gateway.mjs ui",
    "run": "node gateway.mjs start",
    "update": "node gateway.mjs update",
    "upload": "node gateway.mjs upload",
    "status": "node gateway.mjs status",
    "test": "node --test --test-reporter=tap --test-reporter-destination=stdout \"tests/**/*.test.mjs\""
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}