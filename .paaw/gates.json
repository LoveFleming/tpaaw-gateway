{
  "test": {
    "required": true
  },
  "lint": {
    "required": false,
    "level": "warn"
  },
  "clean-tree": {
    "required": false,
    "level": "warn"
  },
  "_hint": "純 Node bootloader（無 build/type-check 步驟）：test 為唯一 hard gate（2026-09-18 release 流程首次套用調整）",
  "build": {
    "required": false,
    "level": "warn",
    "note": "純 Node bootloader 無 build 步驟（2026-09-18）"
  },
  "type-check": {
    "required": false,
    "level": "warn",
    "note": "純 JS 無型別檢查步驟（2026-09-18）"
  }
}