# Dispatch 完整輸出

- Task: TASK-001（範圍限縮版，RR-20260918-1242 release 前）：人工安全 code review — 本次只審 gateway.mjs（release scope 內唯一變更的 code file）；src/server.mjs 已排下週期，不用審。

背景：gateway.mjs 是單檔雙角色（CLI：start/update/status；本地管理 UI：HTTP server 綁 127.0.0.1:4290，9 個 API）。本次 release 變更含：1) F7 CSRF 防護新上線（gateway.mjs:653-681，Host 白名單 + Origin 驗證）；2) F1/F10 版本驗證（VERSION_RE，installVersion/updateLogic）；3) ISS-001 port 佔用偵測。自動掃描 scope 內 0 findings；全庫 6 筆 WARNING 都在 src/server.mjs 且你已 triage 過（誤報/已緩解）。已有 12 個 CSRF 直測全綠（tests/e2e/gateway-ui-csrf.test.mjs）。

審查重點（獨立重找，不依賴掃描器）：
1. CSRF 防護完整性 — Host/Origin 檢查是否涵蓋所有 POST handler 入口、GET 端點是否有狀態改變副作用（若有，GET 不檢查 Origin 就是洞）
2. Command injection — child process 呼叫（spawn/exec）參數是否可控
3. Path traversal — 版本路徑、backup、上傳 zip 安裝（手動上傳 zip 是 9/7 新功能）的檔案路徑驗證
4. DNS rebinding 殘餘風險 — Host 白名單之外還有什麼能繞
5. 其他你認為該看的（secrets、DoS、upstream fetch 的 SSRF）

產出：每個 finding 給 file:line、severity、exploit path；無 finding 也要明確說「審過哪些面、為何放行」。結論給 release verdict：GO / GO with notes / NO-GO。不修碼、不 commit。
- Agent: qa
- 時間: 2026-09-18T11:09:19.784Z
- 長度: 5299 字元（回給呼叫者 500 字預覽）

---

TASK-005 已開給 Priya。以下是正式報告。

---

# 人工安全 Code Review 報告 — gateway.mjs

**Task**: TASK-001（RR-20260918-1242 release 前，範圍限縮版）
**對象**: `gateway.mjs`（單檔雙角色：CLI + 本地管理 UI @127.0.0.1:4290）
**方法**: 全檔逐讀 + `ui/index.html` 渲染層交叉驗證 + 攻擊鏈手動推導，不依賴掃描器
**修復 task**: TASK-005（掛 F20260906-001，high）

---

## Findings（3 筆）

### 🔴 MAJ-001｜Major｜`manifest.version` 未驗證 — F1/F10 只上了一半，update 路徑是洞

**證據**：
- `gateway.mjs:44` — `VERSION_RE` 已定義，但全檔只有 3 個使用點：L123（`listInstalledVersions`）、L355（`installUploadedZip`）。**`installVersion` 與 `updateLogic` 完全沒用**。
- `gateway.mjs:318` — `const v = manifest.version;` 未經任何驗證，直接組三條路徑：
  - L318-319：`zipPath = join(HOME, "logs", \`paaw-${v}.zip\`)`
  - L321-326：`dlUrl = \`${server}/packages/${v}/paaw.zip\``（`v` 可注入 `../`、`?`、`#`）
  - `stageInstall` → `gateway.mjs:294` `join(VERSIONS_DIR, v)` → `"999.0.0/../../.."` resolve 後**寫出 HOME 目錄白名單**
- `gateway.mjs:431` — `writeCurrentAtomic(manifest.version)` 把未驗字串寫進 `current.json`（污染後續所有啟動路徑）
- `gateway.mjs:90-96` — `semverGt` 在**首個差異段**即 return：`"999.0.0/../../x"` 對 `"1.2.3"` 首段 `999 > 1` 直接 `newer=true`，NaN 後段根本不參與比較。我原本預期只有「首次安裝」（`newer = !current` 無檢查）可達，實際上**任何 update 都可觸發**。
- `ui/index.html:280` — `stableVersion.innerHTML = st`，`s.stable` 即 `stable.json` 的 `version` 欄（任意字串）未 escape → stored XSS（同源注入，可再以 UI origin 發合法 POST 繞過 CSRF gate）。

**Exploit path**：攻陷 package server 或 MITM（`POST /api/settings` 允許 `http://` package server，明文可攔）→ `stable.json = {"version":"999.0.0/../../..", "sha256":"<配對自己包>"}` → 使用者按更新 → gateway 寫任意路徑 + `current.json` 污染 + `/api/status` 回傳 payload → UI innerHTML 執行。

**嚴重度權衡（為何是 major 不是 blocker）**：前提是 package server 信任邊界被突破，而該前提下 `sha256` 也來自同一個被污染的 manifest —— 攻擊者本來就有 RCE 等價能力（`npm install` 會跑 package scripts）。此 finding 的真實增量是 **MITM 場景的單點污染 + traversal 寫出白名單 + UI 注入**，屬縱深缺失。但 **spec 一致性問題必須單獨對待**：本次 release 宣稱「F1/F10 已上」與實碼不符 —— 若不改措辭，下週期會以為 update 路徑已有防護。

**修復**（已寫入 TASK-005）：`updateLogic`/`installVersion` 入口 `VERSION_RE.test()` fail-closed（一行）+ `ui/index.html` `s.stable` 過 `esc()`。

---

### 🟡 MED-001｜Medium｜GET 端點無 Host gate → DNS rebinding 讀取面

**證據**：`gateway.mjs:663-681` 的 gate 只攔 `method === "POST"`。GET 端點中：
- `GET /api/paaw-log`（L778-804）— 回傳**完整 PAAW server console log**（可能含路徑、錯誤細節、敏感輸出）
- `GET /api/status`（L706）— 洩漏 `HOME` 絕對路徑、package server URL、版本清單
- `GET /api/settings`（L687）— 洩漏設定值

**Exploit path**：受害者瀏覽攻擊者頁面 → 該頁的域名 rebinding 到 `127.0.0.1` → 對瀏覽器而言請求是 same-origin → 惡意 JS 直接讀 response body。POST 被 Host gate 擋住（rebinding 時 Host = 攻擊者域名），但**讀取面全開**。測試檔註解明載「GET 為讀取操作，不做 Origin/Host 檢查」—— 這是設計決策，但 rebinding 語意下 same-origin 讀取不受 SOP 保護，決策有盲點。

**修法**（TASK-005）：Host gate 套用到所有請求。瀏覽器直連 127.0.0.1 時 Host 必然合法、零誤擋；rebinding 的偽 Host 必被擋。

---

### ⚪ MIN-001｜Minor｜`POST /api/settings` body 累積無上限

`gateway.mjs:712-714` `body += c` 無大小限制（`/api/upload` 有 600MB cap + destroy，防護不對稱）。遠端 DoS 已被 CSRF gate 間接擋住（跨站帶 Origin 必 403），殘餘風險僅本機 process —— 威脅模型外，但補個上限是一行的事。

---

## 已審且放行的面（無 finding，附理由）

| 審查面 | 結論 | 證據 |
|---|---|---|
| **CSRF gate 涵蓋性** | ✅ 6 個 POST handler（settings/update/upload/start/stop/restart）全部在 method 判斷之後、route 分發之前，無繞道入口 | L663-681 gate 結構在 handler 頂部 |
| **GET 副作用** | ✅ 6 個 GET 端點均為純讀取（讀檔/讀狀態/讀 job log），無任何 GET-triggered 寫入或啟動 | L685-804 逐一路由核對 |
| **Command injection** | ✅ 所有 `spawn` 均固定 binary + 固定/版本參數、無 shell 拼接；`version` 進 spawn 前經 `existsSync` 目錄檢查；`openBrowserTab` 的 url 為固定模板 | `startPaaw`、`installDeps`（npm install）、`openBrowserTab` |
| **Path traversal（upload）** | ✅ `installUploadedZip`：manifest 路徑 L355 有 `VERSION_RE`；fallback 檔名 regex `^paaw-(\d+\.\d+\.\d+)\.zip$` 鎖死；`X-Filename`（L803）只進 log 顯示與 regex 判斷，**不進任何路徑** | L349-364 |
| **Zip-slip** | ✅ `extractZip` 對每個 entry 做 `resolve` + `startsWith(destRoot)` 檢查，失敗即 throw | L151-167 |
| **`X-Filename` → XSS 鏈** | ✅ 進 `jobLog` → `renderLog` 有 `<` escape（L250 附近）；`uploadNote` 用 `textContent`；`paawLog` 渲染有完整 `esc()`（`&`/`<`/`>`）| `ui/index.html` |
| **其他 innerHTML 點** | ✅ `chips` 的版本字串經 `VERSION_RE` 過濾（純數字+點）；`act()` 插入的是固定字串 + fetch exception | `ui/index.html` |
| **`GET /api/paaw-log` query** | ✅ 唯一參數 `offset` 有 `parseInt` + `Math.min/max` 夾限，路徑為固定 candidates | L784-786 |
| **`paawHome` 設定** | ✅ 強制絕對路徑格式 + 執行中禁改 + `resolve()` 正規化；屬本地使用者自己的選擇，非攻擊面 | L719-730 |
| **Secrets** | ✅ 無硬編碼憑證；`gateway.json` 只存 packageServer/paawHome/autoUpdate | L47-50 |
| **ISS-001 port 佔用** | ✅ 偵測後 `logFile` 記錄即退出，無 race 視窗（先探測再 listen 的固有窗口對本地工具可接受） | L260-275 |
| **Atomic write** | ✅ `current.json` 與 `gateway.json` 皆 tmp + rename，無半寫狀態 | L72-76, L106-109 |

**Notes（不構成 finding，記錄備查）**：① `PAAW_GW_HOST=0.0.0.0` 部署時 Host gate 可被偽造 Host 繞過 —— 但 Host 檢查本來就只防瀏覽器（瀏覽器強制 Host=連線位址），網路層攻擊者完全控制自己的 header，該情境是「無鑑權公開」屬部署責任，建議文件標註。② `packageServer` 可指向內網 URL，`/api/status` 的 fetch 成敗可當內網探測 oracle —— 設定權限在本地，價值低。③ `npm install` 跑 package scripts、上傳 zip 無 hash 比對 —— 屬 bootloader 對包來源的信任模型（與 npm 生態一致），sha256 驗的是完整性不是善意，為設計決策非缺陷。

---

## Verdict：**GO with notes** ✅（附兩個必要條件）

可 release，但：

1. **Release note 必須修正措辭**：F1/F10 版本驗證實際只涵蓋 upload 與 installed-list，**update 路徑未驗證**。這不是可選項 —— 不修正會讓下週期誤判防護已存在。
2. **TASK-005 需排入**（update 路徑 `VERSION_RE` + UI `stable` escape + GET Host gate）。修復成本極低（三個小點），但以本次 release 的實際風險評估（前提為 package server 邊界突破，該前提下已無新增可達面），不構成 NO-GO 條件。

若你判斷這次 release 的目標環境存在「非 localhost 的 http:// package server」部署，我建議把 TASK-005 提到 push 前完成 —— 這個判斷在你的部署情境資訊內，我手上的資訊不足以替你決定。
